# Основные зависимости
opencv-python==4.8.0.76  # Библиотека для работы с изображениями и видео
numpy==1.24.3            # Библиотека для работы с массивами и математическими операциями
ultralytics==8.0.196     # Библиотека для работы с YOLO
aiohttp==3.8.5           # Библиотека для асинхронных HTTP-запросов (отправка в Telegram)
pyyaml==6.0.1            # Библиотека для работы с YAML-файлами (конфигурация)
asyncio==3.4.3           # Библиотека для асинхронного программирования
logging==0.5.1.2         # Библиотека для логирования
requests==2.31.0         # Библиотека для HTTP-запросов (опционально, если нужны синхронные запросы)

# Дополнительные зависимости для FFmpeg (если используется subprocess)
subprocess32==3.5.4      # Для работы с подпроцессами (опционально, если используется Python < 3.8)

# Дополнительные зависимости для работы с временными файлами
aiofiles==23.2.1         # Асинхронная работа с файлами
